# hz + Thrift 后端脚手架重建实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 使用 `hz v0.9.7` 与 Thrift IDL 默认布局重建 Hertz 脚手架，并重新接入 Eino Graph、Neon `pgxpool`、pgvector、健康检查和 Vercel/本地运行能力。

**Architecture:** `idl/health.thrift` 是 HTTP 契约源，`hz new` 生成默认 `biz/model`、`biz/handler`、`biz/router` 和根路由文件。手写 Handler 只做协议转换，运行时依赖由 `main.go` 装配并通过 Hertz 请求上下文显式传入；Graph、配置和 PostgreSQL 适配继续位于 `internal`。

**Tech Stack:** Go 1.25、hz v0.9.7、Thrift/Thriftgo、Hertz v0.10.5、Eino v0.9.12、pgx/v5 v5.9.2、pgvector-go/pgx v0.4.0。

## Global Constraints

- 必须从 `hz new` 默认布局生成，不使用自定义 layout、handler、model 或 router 目录。
- 正式 HTTP 路由仅由 Thrift IDL 驱动并统一使用 `/api/v1` 前缀。
- 删除 `/ping`、`migrations/`、`DATABASE_MIGRATION_URL` 和迁移 Makefile 目标。
- `biz/model`、`biz/router`、`router_gen.go` 是不可手改生成物。
- Handler 只做协议转换，不直接访问数据库或编排 Graph。
- 依赖通过请求上下文显式传递，不使用包级可变单例。
- 服务启动不强制 `Ping`；数据库状态由 readiness 接口表达。
- 不加入 LLM、Embedding、商品导入、完整 RAG、SSE、购物车或多模态逻辑。
- 所有手写行为先写失败测试；生成代码属于 TDD 例外。
- 每项文件编辑使用 `apply_patch`，保护无关用户改动。

---

## File Structure

- `idl/health.thrift`：健康检查请求、成功响应、错误响应和路由注解。
- `.hz`：hz v0.9.7 默认生成配置。
- `biz/model/health/health.go`：Thrift 生成模型。
- `biz/handler/health/health_service.go`：健康检查协议 Handler。
- `biz/router/health/health.go`、`biz/router/health/middleware.go`：IDL 生成路由和空中间件钩子。
- `biz/router/register.go`、`router_gen.go`：生成路由注册入口。
- `router.go`：保留空的自定义路由入口，不注册 `/ping`。
- `internal/http/dependencies.go`：请求级依赖类型与提取函数。
- `internal/http/server.go`：Hertz 选项、中间件、稳定错误映射。
- `router_test.go`：通过根生成路由验证 HTTP 契约。
- `main.go`、`main_test.go`：进程装配、监听和优雅关闭。
- `internal/config/*`、`internal/agent/*`、`internal/platform/postgres/*`：保留并重新纳入新模块依赖。
- `Makefile`、`.env.example`、`.gitignore`、`README.md`、`AGENTS.md`：开发和项目契约。

---

### Task 1: 使用 Thrift IDL 生成默认 hz 项目

**Files:**
- Create: `idl/health.thrift`
- Generate: `.hz`
- Generate: `biz/model/health/health.go`
- Generate: `biz/handler/health/health_service.go`
- Generate: `biz/router/health/health.go`
- Generate: `biz/router/health/middleware.go`
- Generate: `biz/router/register.go`
- Generate: `main.go`
- Generate: `router.go`
- Generate: `router_gen.go`
- Generate: `go.mod`
- Delete: `internal/http/app.go`
- Delete: `internal/http/health_test.go`
- Delete: `main_test.go`
- Delete: `migrations/000001_init.up.sql`
- Delete: `migrations/000001_init.down.sql`
- Regenerate: `.gitignore`
- Regenerate: `go.sum`

**Interfaces:**
- Produces: Thrift `HealthService` with `Live` and `Ready` HTTP routes.
- Produces: generated Go models `health.HealthRequest`, `health.HealthResponse`, `health.ErrorResponse`.
- Produces: root `register(*server.Hertz)` that invokes `router.GeneratedRegister` and `customizedRegister`.

- [ ] **Step 1: 创建完整 Thrift IDL**

Use `apply_patch` to create `idl/health.thrift`:

```thrift
namespace go health

struct HealthRequest {}

struct HealthResponse {
    1: required string Status (go.tag="json:\"status\"")
    2: required string Code (go.tag="json:\"code\"")
}

struct ErrorResponse {
    1: required string Status (go.tag="json:\"status\"")
    2: required string Code (go.tag="json:\"code\"")
    3: required string Message (go.tag="json:\"message\"")
    4: required string RequestID (go.tag="json:\"request_id\"")
}

service HealthService {
    HealthResponse Live(1: HealthRequest request) (api.get="/api/v1/health/live")
    HealthResponse Ready(1: HealthRequest request) (api.get="/api/v1/health/ready")
}
```

- [ ] **Step 2: 删除旧手写 HTTP 脚手架与迁移文件**

Use `apply_patch` to delete `main.go`, `main_test.go`, `internal/http/app.go`, `internal/http/health_test.go`, `go.mod`, `go.sum`, `.gitignore`, and both files under `migrations/`. Remove the empty `migrations` directory after its files are gone. Preserve `internal/agent`, `internal/config`, and `internal/platform/postgres` unchanged.

- [ ] **Step 3: 运行 hz new**

Run:

```bash
/Users/polaris/go/bin/hz new --idl idl/health.thrift --module github.com/PolarishT/sales-agent --service sales-agent --sort_router
```

Expected: exit 0; `.hz`, `biz/`, `main.go`, `router.go`, `router_gen.go`, `go.mod`, `.gitignore`, `build.sh`, and `script/bootstrap.sh` exist. Generated paths use `biz/handler/health`, `biz/model/health`, and `biz/router/health`.

- [ ] **Step 4: 删除非 IDL 的 ping 示例**

Use `apply_patch` to delete `biz/handler/ping.go` and replace `router.go` with:

```go
// Code generated by hertz generator.

package main

import "github.com/cloudwego/hertz/pkg/app/server"

// customizedRegister registers routes that cannot be expressed by IDL.
func customizedRegister(_ *server.Hertz) {}
```

- [ ] **Step 5: 恢复项目依赖并整理模块**

Run:

```bash
go get github.com/cloudwego/eino@v0.9.12
go get github.com/cloudwego/hertz@v0.10.5
go get github.com/google/uuid@v1.6.0
go get github.com/jackc/pgx/v5@v5.9.2
go get github.com/pgvector/pgvector-go/pgx@v0.4.0
go mod tidy
```

Expected: exit 0; `go.mod` retains module `github.com/PolarishT/sales-agent`, the Thrift replacement generated by hz, and the exact direct dependency versions above.

- [ ] **Step 6: 验证生成基线可编译**

Run: `go test ./...`

Expected: PASS. Existing config, Graph, and PostgreSQL tests pass; generated Handler stubs compile.

- [ ] **Step 7: 提交生成基线**

```bash
git add .hz .gitignore build.sh script idl biz main.go router.go router_gen.go go.mod go.sum internal migrations
git commit -m "chore: 使用 hz 生成 thrift 项目骨架"
```

Expected: commit succeeds; the deletion of `migrations/` is included.

---

### Task 2: 通过生成路由实现健康检查与错误契约

**Files:**
- Create: `router_test.go`
- Create: `internal/http/dependencies.go`
- Create: `internal/http/server.go`
- Modify: `biz/handler/health/health_service.go`

**Interfaces:**
- Consumes: generated `register(*server.Hertz)` and generated health models.
- Produces: `httpapi.HealthChecker` with `Ping(context.Context) error`.
- Produces: `httpapi.AgentRunner` with `Invoke(context.Context, agent.Request) (agent.Response, error)`.
- Produces: `httpapi.Dependencies{HealthChecker, AgentRunner, ReadinessTimeout}`.
- Produces: `httpapi.NewServer(httpapi.Options) *server.Hertz`.
- Produces: `httpapi.DependenciesFrom(*app.RequestContext) (Dependencies, bool)`.
- Produces: `httpapi.WriteError(*app.RequestContext, int, string, string, string)`.

- [ ] **Step 1: 编写根路由失败测试**

Use `apply_patch` to create `router_test.go`:

```go
package main

import (
	"context"
	"encoding/json"
	"errors"
	"strings"
	"testing"
	"time"

	"github.com/PolarishT/sales-agent/internal/agent"
	httpapi "github.com/PolarishT/sales-agent/internal/http"
	"github.com/cloudwego/hertz/pkg/app"
	"github.com/cloudwego/hertz/pkg/app/server"
	"github.com/cloudwego/hertz/pkg/common/ut"
)

type fakeHealthChecker struct {
	err   error
	calls int
}

func (f *fakeHealthChecker) Ping(context.Context) error {
	f.calls++
	return f.err
}

type fakeAgentRunner struct{}

func (fakeAgentRunner) Invoke(_ context.Context, request agent.Request) (agent.Response, error) {
	return agent.Response{Query: request.Query, Stage: "fake"}, nil
}

func TestGeneratedLiveRouteDoesNotCheckDatabase(t *testing.T) {
	checker := &fakeHealthChecker{err: errors.New("database is unavailable")}
	h := newTestServer(checker)

	response := ut.PerformRequest(h.Engine, "GET", "/api/v1/health/live", nil)
	if response.Code != 200 {
		t.Fatalf("status = %d, want 200", response.Code)
	}
	if checker.calls != 0 {
		t.Fatalf("Ping() calls = %d, want 0", checker.calls)
	}
	assertJSONField(t, response.Body.Bytes(), "status", "ok")
	assertJSONField(t, response.Body.Bytes(), "code", "LIVE")
}

func TestGeneratedReadyRoute(t *testing.T) {
	tests := []struct {
		name       string
		pingError  error
		wantStatus int
		wantCode   string
	}{
		{name: "database is ready", wantStatus: 200, wantCode: "READY"},
		{name: "database is unavailable", pingError: errors.New("offline"), wantStatus: 503, wantCode: "DATABASE_UNAVAILABLE"},
	}

	for _, tc := range tests {
		t.Run(tc.name, func(t *testing.T) {
			checker := &fakeHealthChecker{err: tc.pingError}
			h := newTestServer(checker)
			response := ut.PerformRequest(h.Engine, "GET", "/api/v1/health/ready", nil)
			if response.Code != tc.wantStatus {
				t.Fatalf("status = %d, want %d", response.Code, tc.wantStatus)
			}
			if checker.calls != 1 {
				t.Fatalf("Ping() calls = %d, want 1", checker.calls)
			}
			assertJSONField(t, response.Body.Bytes(), "code", tc.wantCode)
		})
	}
}

func TestGeneratedRoutesExcludePing(t *testing.T) {
	h := newTestServer(&fakeHealthChecker{})
	response := ut.PerformRequest(h.Engine, "GET", "/ping", nil)
	if response.Code != 404 {
		t.Fatalf("status = %d, want 404", response.Code)
	}
}

func TestStableTransportErrors(t *testing.T) {
	h := newTestServer(&fakeHealthChecker{})

	notFound := ut.PerformRequest(h.Engine, "GET", "/missing", nil)
	if notFound.Code != 404 {
		t.Fatalf("404 status = %d, want 404", notFound.Code)
	}
	requestID := string(notFound.Result().Header.Peek("X-Request-ID"))
	if requestID == "" {
		t.Fatal("X-Request-ID header is empty")
	}
	assertJSONField(t, notFound.Body.Bytes(), "code", "NOT_FOUND")
	assertJSONField(t, notFound.Body.Bytes(), "request_id", requestID)

	wrongMethod := ut.PerformRequest(h.Engine, "POST", "/api/v1/health/live", nil)
	if wrongMethod.Code != 405 {
		t.Fatalf("405 status = %d, want 405", wrongMethod.Code)
	}
	assertJSONField(t, wrongMethod.Body.Bytes(), "code", "METHOD_NOT_ALLOWED")
}

func TestRecoveryDoesNotExposePanic(t *testing.T) {
	h := newTestServer(&fakeHealthChecker{})
	h.GET("/panic", func(_ context.Context, _ *app.RequestContext) {
		panic("sensitive internal detail")
	})

	response := ut.PerformRequest(h.Engine, "GET", "/panic", nil)
	if response.Code != 500 {
		t.Fatalf("status = %d, want 500", response.Code)
	}
	assertJSONField(t, response.Body.Bytes(), "code", "INTERNAL_ERROR")
	if strings.Contains(string(response.Body.Bytes()), "sensitive internal detail") {
		t.Fatal("response exposed panic detail")
	}
}

func TestReadyWithoutDependenciesUsesStableError(t *testing.T) {
	h := httpapi.NewServer(httpapi.Options{Address: ":0"})
	register(h)
	response := ut.PerformRequest(h.Engine, "GET", "/api/v1/health/ready", nil)
	if response.Code != 503 {
		t.Fatalf("status = %d, want 503", response.Code)
	}
	assertJSONField(t, response.Body.Bytes(), "code", "DATABASE_UNAVAILABLE")
}

func newTestServer(checker httpapi.HealthChecker) *server.Hertz {
	h := httpapi.NewServer(httpapi.Options{
		Address: ":0",
		Dependencies: httpapi.Dependencies{
			HealthChecker:    checker,
			AgentRunner:      fakeAgentRunner{},
			ReadinessTimeout: time.Second,
		},
	})
	register(h)
	return h
}

func assertJSONField(t *testing.T, body []byte, field, want string) {
	t.Helper()
	var payload map[string]any
	if err := json.Unmarshal(body, &payload); err != nil {
		t.Fatalf("decode response %q: %v", body, err)
	}
	if got := payload[field]; got != want {
		t.Fatalf("%s = %v, want %q", field, got, want)
	}
}
```

- [ ] **Step 2: 运行测试并确认红灯**

Run: `go test . -run 'TestGenerated|TestStable|TestRecovery|TestReadyWithout' -v`

Expected: FAIL to compile because `internal/http`, `httpapi.NewServer`, and related interfaces do not exist after Task 1.

- [ ] **Step 3: 实现显式请求依赖**

Use `apply_patch` to create `internal/http/dependencies.go`:

```go
package httpapi

import (
	"context"
	"time"

	"github.com/PolarishT/sales-agent/internal/agent"
	"github.com/cloudwego/hertz/pkg/app"
)

const dependenciesKey = "application_dependencies"

type HealthChecker interface {
	Ping(context.Context) error
}

type AgentRunner interface {
	Invoke(context.Context, agent.Request) (agent.Response, error)
}

type Dependencies struct {
	HealthChecker    HealthChecker
	AgentRunner      AgentRunner
	ReadinessTimeout time.Duration
}

func DependenciesFrom(ctx *app.RequestContext) (Dependencies, bool) {
	value, ok := ctx.Get(dependenciesKey)
	if !ok {
		return Dependencies{}, false
	}
	dependencies, ok := value.(Dependencies)
	return dependencies, ok
}

func dependenciesMiddleware(dependencies Dependencies) app.HandlerFunc {
	return func(ctx context.Context, requestContext *app.RequestContext) {
		requestContext.Set(dependenciesKey, dependencies)
		requestContext.Next(ctx)
	}
}
```

- [ ] **Step 4: 实现 Hertz 服务与错误中间件**

Use `apply_patch` to create `internal/http/server.go`:

```go
package httpapi

import (
	"context"
	"log/slog"
	"net"
	"time"

	"github.com/PolarishT/sales-agent/biz/model/health"
	"github.com/cloudwego/hertz/pkg/app"
	"github.com/cloudwego/hertz/pkg/app/middlewares/server/recovery"
	"github.com/cloudwego/hertz/pkg/app/server"
	"github.com/cloudwego/hertz/pkg/protocol/consts"
	"github.com/google/uuid"
)

const (
	defaultReadinessTimeout = 2 * time.Second
	defaultRequestTimeout   = 30 * time.Second
	defaultShutdownTimeout  = 10 * time.Second
)

type Options struct {
	Address         string
	RequestTimeout  time.Duration
	ShutdownTimeout time.Duration
	Listener        net.Listener
	Dependencies    Dependencies
}

func NewServer(options Options) *server.Hertz {
	requestTimeout := options.RequestTimeout
	if requestTimeout <= 0 {
		requestTimeout = defaultRequestTimeout
	}
	shutdownTimeout := options.ShutdownTimeout
	if shutdownTimeout <= 0 {
		shutdownTimeout = defaultShutdownTimeout
	}
	if options.Dependencies.ReadinessTimeout <= 0 {
		options.Dependencies.ReadinessTimeout = defaultReadinessTimeout
	}

	listenOption := server.WithHostPorts(options.Address)
	if options.Listener != nil {
		listenOption = server.WithListener(options.Listener)
	}
	h := server.New(
		listenOption,
		server.WithReadTimeout(requestTimeout),
		server.WithWriteTimeout(requestTimeout),
		server.WithExitWaitTime(shutdownTimeout),
		server.WithHandleMethodNotAllowed(true),
	)
	h.Use(
		requestIDMiddleware(),
		dependenciesMiddleware(options.Dependencies),
		recovery.Recovery(recovery.WithRecoveryHandler(recoveryHandler)),
	)
	h.NoRoute(func(_ context.Context, ctx *app.RequestContext) {
		WriteError(ctx, consts.StatusNotFound, "error", "NOT_FOUND", "请求的资源不存在")
	})
	h.NoMethod(func(_ context.Context, ctx *app.RequestContext) {
		WriteError(ctx, consts.StatusMethodNotAllowed, "error", "METHOD_NOT_ALLOWED", "请求方法不受支持")
	})
	return h
}

func WriteError(ctx *app.RequestContext, statusCode int, status, code, message string) {
	ctx.JSON(statusCode, &health.ErrorResponse{
		Status:    status,
		Code:      code,
		Message:   message,
		RequestID: ctx.GetString("request_id"),
	})
}

func requestIDMiddleware() app.HandlerFunc {
	return func(ctx context.Context, requestContext *app.RequestContext) {
		requestID := uuid.NewString()
		requestContext.Set("request_id", requestID)
		requestContext.Response.Header.Set("X-Request-ID", requestID)
		requestContext.Next(ctx)
	}
}

func recoveryHandler(_ context.Context, ctx *app.RequestContext, _ any, _ []byte) {
	slog.Error("请求处理发生 panic", "request_id", ctx.GetString("request_id"))
	ctx.Abort()
	WriteError(ctx, consts.StatusInternalServerError, "error", "INTERNAL_ERROR", "服务内部错误")
}
```

- [ ] **Step 5: 将生成 Handler 改为薄协议适配器**

Use `apply_patch` to replace `biz/handler/health/health_service.go` with:

```go
// Code generated by hertz generator.

package health

import (
	"context"

	healthmodel "github.com/PolarishT/sales-agent/biz/model/health"
	httpapi "github.com/PolarishT/sales-agent/internal/http"
	"github.com/cloudwego/hertz/pkg/app"
	"github.com/cloudwego/hertz/pkg/protocol/consts"
)

// Live reports whether the process can serve HTTP requests.
func Live(_ context.Context, ctx *app.RequestContext) {
	ctx.JSON(consts.StatusOK, &healthmodel.HealthResponse{Status: "ok", Code: "LIVE"})
}

// Ready reports whether the runtime database dependency is reachable.
func Ready(ctx context.Context, requestContext *app.RequestContext) {
	dependencies, ok := httpapi.DependenciesFrom(requestContext)
	if !ok || dependencies.HealthChecker == nil {
		httpapi.WriteError(requestContext, consts.StatusServiceUnavailable, "unavailable", "DATABASE_UNAVAILABLE", "数据库暂不可用")
		return
	}

	checkContext, cancel := context.WithTimeout(ctx, dependencies.ReadinessTimeout)
	defer cancel()
	if err := dependencies.HealthChecker.Ping(checkContext); err != nil {
		httpapi.WriteError(requestContext, consts.StatusServiceUnavailable, "unavailable", "DATABASE_UNAVAILABLE", "数据库暂不可用")
		return
	}

	requestContext.JSON(consts.StatusOK, &healthmodel.HealthResponse{Status: "ok", Code: "READY"})
}
```

- [ ] **Step 6: 运行聚焦测试并确认绿灯**

Run: `go test . -run 'TestGenerated|TestStable|TestRecovery|TestReadyWithout' -v`

Expected: PASS. `/ping` is 404, live never calls `Ping`, ready calls it once, and stable errors contain request IDs.

- [ ] **Step 7: 运行全部测试**

Run: `go test ./...`

Expected: PASS.

- [ ] **Step 8: 提交 HTTP 契约实现**

```bash
git add router_test.go internal/http biz/handler/health/health_service.go
git commit -m "feat: 实现 idl 健康检查路由"
```

---

### Task 3: 接入配置、Graph、数据库池与服务生命周期

**Files:**
- Create: `main_test.go`
- Modify: `main.go`
- Modify: `internal/platform/postgres/pool.go`

**Interfaces:**
- Consumes: `config.Load`, `postgres.NewPool`, `agent.NewGraph`, `httpapi.NewServer`, generated `register`.
- Produces: `run() error`, `openListener(string) (net.Listener, error)`, `serve(context.Context, *server.Hertz, time.Duration) error`.

- [ ] **Step 1: 编写监听器失败测试**

Use `apply_patch` to create `main_test.go`:

```go
package main

import (
	"net"
	"testing"
)

func TestOpenListenerReturnsErrorWhenAddressIsInUse(t *testing.T) {
	occupied, err := net.Listen("tcp", "127.0.0.1:0")
	if err != nil {
		t.Fatalf("占用测试端口: %v", err)
	}
	defer occupied.Close()

	listener, err := openListener(occupied.Addr().String())
	if err == nil {
		listener.Close()
		t.Fatal("openListener() error = nil, want an error")
	}
}
```

- [ ] **Step 2: 运行测试并确认红灯**

Run: `go test . -run TestOpenListenerReturnsErrorWhenAddressIsInUse -v`

Expected: FAIL to compile because the hz-generated `main.go` has no `openListener`.

- [ ] **Step 3: 实现进程装配且不在启动时 Ping**

Use `apply_patch` to replace `main.go` with:

```go
package main

import (
	"context"
	"errors"
	"fmt"
	"log/slog"
	"net"
	"os"
	"os/signal"
	"strings"
	"syscall"
	"time"

	"github.com/PolarishT/sales-agent/internal/agent"
	"github.com/PolarishT/sales-agent/internal/config"
	httpapi "github.com/PolarishT/sales-agent/internal/http"
	postgresplatform "github.com/PolarishT/sales-agent/internal/platform/postgres"
	"github.com/cloudwego/hertz/pkg/app/server"
)

func main() {
	if err := run(); err != nil {
		slog.Error("服务启动失败", "error", err)
		os.Exit(1)
	}
}

func run() error {
	settings, err := config.Load(os.Getenv)
	if err != nil {
		return fmt.Errorf("加载配置: %w", err)
	}
	configureLogger(settings.LogLevel)

	pool, err := postgresplatform.NewPool(context.Background(), settings.DatabaseURL, postgresplatform.Options{
		MaxConns:        settings.DatabaseMaxConns,
		MinConns:        settings.DatabaseMinConns,
		MaxConnIdleTime: settings.DatabaseMaxConnIdleTime,
	})
	if err != nil {
		return err
	}
	defer pool.Close()

	graph, err := agent.NewGraph(context.Background())
	if err != nil {
		return err
	}

	listener, err := openListener(settings.Address)
	if err != nil {
		return err
	}
	defer listener.Close()

	h := httpapi.NewServer(httpapi.Options{
		Address:         settings.Address,
		RequestTimeout:  settings.RequestTimeout,
		ShutdownTimeout: settings.ShutdownTimeout,
		Listener:        listener,
		Dependencies: httpapi.Dependencies{
			HealthChecker:    pool,
			AgentRunner:      graph,
			ReadinessTimeout: settings.DatabaseConnectTimeout,
		},
	})
	register(h)

	slog.Info("导购后端已监听", "environment", settings.Environment, "address", listener.Addr().String())
	signalContext, stopSignals := signal.NotifyContext(context.Background(), syscall.SIGHUP, syscall.SIGINT, syscall.SIGTERM)
	defer stopSignals()
	return serve(signalContext, h, settings.ShutdownTimeout)
}

func openListener(address string) (net.Listener, error) {
	listener, err := net.Listen("tcp", address)
	if err != nil {
		return nil, fmt.Errorf("监听地址 %q: %w", address, err)
	}
	return listener, nil
}

func serve(ctx context.Context, h *server.Hertz, shutdownTimeout time.Duration) error {
	runErrors := make(chan error, 1)
	go func() {
		runErrors <- h.Run()
	}()

	select {
	case err := <-runErrors:
		if err == nil {
			return errors.New("Hertz 服务意外停止")
		}
		return fmt.Errorf("运行 Hertz 服务: %w", err)
	case <-ctx.Done():
		shutdownContext, cancelShutdown := context.WithTimeout(context.Background(), shutdownTimeout)
		defer cancelShutdown()
		if err := h.Shutdown(shutdownContext); err != nil {
			return fmt.Errorf("关闭 Hertz 服务: %w", err)
		}
		return nil
	}
}

func configureLogger(rawLevel string) {
	level := slog.LevelInfo
	switch strings.ToLower(strings.TrimSpace(rawLevel)) {
	case "debug":
		level = slog.LevelDebug
	case "warn", "warning":
		level = slog.LevelWarn
	case "error":
		level = slog.LevelError
	}
	slog.SetDefault(slog.New(slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{Level: level})))
}
```

- [ ] **Step 4: 修正连接池注释以匹配启动行为**

Use `apply_patch` to replace the `NewPool` comment in `internal/platform/postgres/pool.go` with:

```go
// NewPool 创建连接池，但不会主动发起数据库连接。
// 数据库可用性由 HTTP readiness 检查在受限超时内判断。
```

- [ ] **Step 5: 运行聚焦与全量测试**

Run:

```bash
go test . -run TestOpenListenerReturnsErrorWhenAddressIsInUse -v
go test ./...
```

Expected: both commands PASS. No startup code calls `pool.Ping` before Hertz starts.

- [ ] **Step 6: 提交生命周期装配**

```bash
git add main.go main_test.go internal/platform/postgres/pool.go
git commit -m "feat: 装配 hz 服务运行时依赖"
```

---

### Task 4: 更新生成命令、环境契约和中文文档

**Files:**
- Modify: `Makefile`
- Modify: `.env.example`
- Modify: `.gitignore`
- Modify: `README.md`
- Modify: `AGENTS.md`

**Interfaces:**
- Produces: `make generate`, `make fmt`, `make test`, `make vet`, `make build`, `make run`, `make vercel-dev`.
- Removes: `make migrate-up`, `make migrate-down`, `DATABASE_MIGRATION_URL` documentation.

- [ ] **Step 1: 更新 Makefile**

Use `apply_patch` to replace `Makefile` with:

```make
HZ ?= hz
GO_FILES := $(shell find . -type f -name '*.go' -not -path './vendor/*')

.PHONY: generate fmt test vet build run vercel-dev

generate:
	$(HZ) update --idl idl/health.thrift --sort_router

fmt:
	gofmt -w $(GO_FILES)

test:
	go test ./...

vet:
	go vet ./...

build:
	go build -o "$${TMPDIR:-/tmp}/sales-agent-server" .

run:
	@set -a; \
	if [ -f .env.local ]; then . ./.env.local; elif [ -f .env ]; then . ./.env; fi; \
	set +a; \
	go run .

vercel-dev:
	vercel dev
```

- [ ] **Step 2: 更新安全环境变量示例**

Use `apply_patch` to replace `.env.example` with:

```dotenv
APP_ENV=development
HTTP_ADDR=:3000
DATABASE_URL=postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DATABASE?sslmode=require
DB_MAX_CONNS=4
DB_MIN_CONNS=0
DB_MAX_CONN_IDLE_TIME=30s
DB_CONNECT_TIMEOUT=5s
REQUEST_TIMEOUT=30s
GRAPH_TIMEOUT=60s
SHUTDOWN_TIMEOUT=10s
LOG_LEVEL=info
```

- [ ] **Step 3: 补齐生成 `.gitignore` 的秘密文件规则**

Use `apply_patch` to add the following exact block to `.gitignore`:

```gitignore
.env
.env.*
!.env.example
```

Keep the remaining hz-generated ignore rules unchanged.

- [ ] **Step 4: 重写 README 的使用说明**

Use `apply_patch` to replace `README.md` with a concise Chinese document containing these exact operational facts:

```markdown
# AI 电商导购后端

这是一个使用 Hertz、Eino Graph、Neon Postgres 和 pgvector 的 AI 电商导购后端骨架。HTTP 契约由 Thrift IDL 定义，项目脚手架和路由由 `hz` 生成。

当前只包含 `hz` 默认项目结构、存活与就绪检查、最小 Eino Graph、Neon 连接池和 pgvector 类型注册。不包含数据库迁移、LLM、Embedding、商品导入、完整 RAG、SSE、购物车或多模态逻辑。

## 生成代码

首次生成使用 hz v0.9.7：

```bash
hz new --idl idl/health.thrift --module github.com/PolarishT/sales-agent --service sales-agent --sort_router
```

仓库已经包含生成结果。修改 `idl/health.thrift` 后运行：

```bash
make generate
```

`biz/model`、`biz/router` 和 `router_gen.go` 是生成物，不要手工修改。项目使用 hz 默认目录，不使用自定义 layout。

## 本地运行

需要 Go 1.25、hz v0.9.7，以及已经准备好 pgvector 扩展和业务表的 Neon 数据库。本仓库不执行数据库迁移。

```bash
cp .env.example .env.local
# 编辑 .env.local 中的 DATABASE_URL
make run
```

验证接口：

```bash
curl http://localhost:3000/api/v1/health/live
curl http://localhost:3000/api/v1/health/ready
```

`live` 只检查 HTTP 进程；`ready` 在受限超时内检查 Neon。数据库不可用不会阻止 HTTP 服务启动，但 `ready` 会返回 HTTP 503。

## 环境变量

`DATABASE_URL` 是唯一必需的运行时变量，必须使用 Neon 池化地址。监听地址优先级为 `PORT`、`HTTP_ADDR`、`:3000`；环境优先级为 `APP_ENV`、`VERCEL_ENV`、`development`。

连接池和超时可通过 `DB_MAX_CONNS`、`DB_MIN_CONNS`、`DB_MAX_CONN_IDLE_TIME`、`DB_CONNECT_TIMEOUT`、`REQUEST_TIMEOUT`、`GRAPH_TIMEOUT`、`SHUTDOWN_TIMEOUT` 和 `LOG_LEVEL` 调整。`PORT` 和 `VERCEL_*` 由 Vercel 注入，不写入 `.env.example`。

## Vercel

Production、Preview 和 Development 分别配置 `DATABASE_URL`。Preview 必须使用独立 Neon 分支，不能默认连接生产数据库。

```bash
vercel deploy
vercel deploy --prod
```

## 验证命令

```bash
make generate
make fmt
git diff --check
make test
make vet
make build
```
```

- [ ] **Step 5: 更新 AGENTS.md 阶段和目录规则**

Use `apply_patch` to make these exact semantic changes in `AGENTS.md`:

1. Replace the current-stage migration bullet with `hz v0.9.7 默认脚手架与 Thrift IDL 契约；`.
2. Replace the directory section so `idl` is the HTTP contract source; `biz/model`, `biz/router`, and `router_gen.go` are generated and immutable; `biz/handler` is the protocol adapter; `main.go` is dependency assembly; `internal/agent`, `internal/config`, `internal/http`, and `internal/platform` retain their approved responsibilities.
3. State that first generation uses `hz new`, later IDL changes use `hz update`, and custom hz layouts are not used.
4. Remove the requirement for initial migrations and all `DATABASE_MIGRATION_URL` rules.
5. State that the service connects only to a database already prepared outside this repository.
6. Add `make generate` before `make fmt` in completion verification.

- [ ] **Step 6: 验证迁移内容已完全移除**

Run:

```bash
test ! -d migrations
! rg -n 'DATABASE_MIGRATION_URL|migrate-up|migrate-down' AGENTS.md README.md .env.example Makefile
```

Expected: both commands exit 0.

- [ ] **Step 7: 提交工具与文档**

```bash
git add Makefile .env.example .gitignore README.md AGENTS.md
git commit -m "docs: 说明 hz 生成与运行方式"
```

---

### Task 5: 验证生成稳定性与完整构建

**Files:**
- Verify: all tracked files
- Modify only if a verification command exposes a defect within this specification.

**Interfaces:**
- Verifies: IDL regeneration, formatting, tests, vet, build, migration removal, and clean generated diff.

- [ ] **Step 1: 运行 hz update 并检查生成差异**

Run:

```bash
make generate HZ=/Users/polaris/go/bin/hz
git diff -- biz/model biz/router router_gen.go .hz
```

Expected: generation exits 0 and generated-only diff is empty. Handler changes remain intact because `hz update` preserves existing Handler functions.

- [ ] **Step 2: 运行格式化和差异检查**

Run:

```bash
make fmt
git diff --check
```

Expected: both commands exit 0.

- [ ] **Step 3: 运行测试、vet 和构建**

Run:

```bash
make test
make vet
make build
```

Expected: all commands exit 0. Build output is written below `${TMPDIR:-/tmp}`, not the repository.

- [ ] **Step 4: 检查范围与秘密文件**

Run:

```bash
test ! -d migrations
! rg -n 'DATABASE_MIGRATION_URL|migrate-up|migrate-down' AGENTS.md README.md .env.example Makefile
git status --short
```

Expected: migration checks exit 0. Status contains only intentional files from this plan and no `.env`, credentials, build output, or unrelated changes.

- [ ] **Step 5: 提交验证产生的必要格式调整**

If `make fmt` changed tracked source files, commit only those formatting changes:

```bash
git add main.go main_test.go router.go router_test.go biz internal
git commit -m "style: 统一 hz 项目代码格式"
```

If formatting produced no diff, do not create an empty commit.

- [ ] **Step 6: 汇总最终状态**

Run:

```bash
git log -5 --oneline
git status --short --branch
```

Expected: the branch contains focused Chinese commits for generation, HTTP behavior, runtime assembly, and documentation; the worktree is clean.

---

## Plan Self-Review

- Spec coverage: default `hz new`, Thrift routes, generated ownership, `/ping` removal, runtime dependencies, no startup Ping, migration removal, docs, and all required verification commands are mapped to tasks.
- Type consistency: `httpapi.Dependencies`, `httpapi.Options`, `httpapi.NewServer`, `httpapi.DependenciesFrom`, generated health models, and root `register` use identical names across tasks.
- Scope: no later-phase AI, RAG, SSE, transaction, or multimodal behavior is introduced.
- Generated-code exception: only hz/thriftgo output bypasses test-first; every handwritten behavior has a failing test step.
